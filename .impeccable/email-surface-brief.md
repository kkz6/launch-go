Scope: every transactional HTML email rendered by `internal/pkg/mail/templates`, with direct notification callers inheriting the shared shell. Visitor mode: Operate.

Audience and job: developers and operators must understand what happened, which resource is affected, and the next useful action without opening the application first. Account recipients must complete authentication and invitation actions confidently.

Content and constraints: preserve every existing business fact, link, plain-text alternative, dynamic-value escaping, and email-client fallback. Support Outlook-safe table markup, stripped head CSS, long technical values, and mobile widths.

Chosen direction: Execution Timeline. One continuous white execution sheet uses a single one-pixel lifecycle line, state nodes, status-only color, Plus Jakarta Sans interface text, and JetBrains Mono only for identifiers, measurements, and output. Lightweight emails become one-state timelines; operational messages add context and evidence states.

Memorable moment: the current event state and action occupy one uninterrupted scan path, while completed context remains visible but quiet.

Approved comp: `.impeccable/mocks/decision/timeline-decision.png`.

Unresolved decisions: none.
