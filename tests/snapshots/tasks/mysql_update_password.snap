#!/bin/bash
set -euo pipefail
trap '' PIPE
export DEBIAN_FRONTEND=noninteractive


sudo mysql --user="root" --password="rootpassword" -e "ALTER USER 'appuser'@'%' IDENTIFIED BY 'newpassword123';"

sudo mysql --user="root" --password="rootpassword" -e "FLUSH PRIVILEGES;"
